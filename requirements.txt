# ---------------------------------------------------------------------------
# Runtime dependencies for the Cardiac Opportunity Agent.
# Pinned to compatible ranges rather than exact versions so the project keeps
# installing cleanly for the duration of the competition.
# ---------------------------------------------------------------------------

# --- Core data + analytics -------------------------------------------------
pandas>=2.2,<3.0
numpy>=1.26,<3.0
duckdb>=1.0,<2.0
openpyxl>=3.1,<4.0
pyarrow>=15.0

# --- Configuration ---------------------------------------------------------
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
PyYAML>=6.0,<7.0
python-dotenv>=1.0,<2.0

# --- LLM providers ---------------------------------------------------------
# Anthropic is the primary provider. The others are optional; the provider
# layer degrades gracefully if they are not installed.
anthropic>=0.69,<1.0

# --- Service + interface ---------------------------------------------------
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
streamlit>=1.36,<2.0
plotly>=5.22,<7.0

# --- CLI + logging ---------------------------------------------------------
# typer below 0.15 imports `get_terminal_size` from click.termui, which click
# removed. Pinning the floor here keeps a fresh install from resolving a
# combination that fails at import time.
typer>=0.15,<1.0
click>=8.1,<9.0
rich>=13.7,<15.0
structlog>=24.1,<26.0
